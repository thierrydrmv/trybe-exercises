// 1. Crie uma constante chamada base e uma variável chamada height e atribua os seus respectivos valores: 5 e 8.
const base = 5;
let height = 8;
// 2. Crie uma constante chamada area e atribua a ela o resultado da multiplicação da base pela altura. Dica: 
// lembre-se de usar sempre o console.log() para imprimir as variáveis e checar os resultados das operações!

const area = height*base;
console.log(area)

// 3. Crie uma constante chamada perimeter e atribua a ela a soma de todos os lados do retângulo.
const perimeter = base + base + height + height
console.log(perimeter)